<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fusion1events"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminMainMenuActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".EventCreationActivity" />
        <activity android:name=".EventsPageActivity" />
        <activity android:name=".EventDetailsActivity" />
        <activity android:name=".FacilityPageActivity"/>
        <activity android:name=".FacilityAddActivity"/>
        <activity android:name=".FacilityEditActivity"/>
        <activity android:name=".ScanQRCodeActivity"/>
        <activity android:name=".NotificationPageActivity"/>
        <activity android:name=".ManageEventActivity"/>
        <activity android:name=".EventUpdateActivity"/>
        <activity android:name=".AdminEventActivity"
            />
    </application>

</manifest>